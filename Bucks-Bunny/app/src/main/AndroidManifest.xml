<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tapan.wallet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".secondpage"/>
        <activity android:name=".signinpage"/>
        <activity android:name=".GroupPage"/>
        <activity android:name=".Categories"/>
        <activity android:name=".ExpenseChart"/>
        <activity android:name=".ExpenseInput"/>
        <activity android:name=".GroupExpenseInput"/>
        <activity android:name=".tapan"/>
        <activity android:name=".spliting"/>
    </application>

</manifest>
